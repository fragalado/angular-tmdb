import { MovieOriginalLanguagePipe } from './movie-original-language.pipe';

describe('MovieOriginalLanguagePipe', () => {
  it('create an instance', () => {
    const pipe = new MovieOriginalLanguagePipe();
    expect(pipe).toBeTruthy();
  });
});
